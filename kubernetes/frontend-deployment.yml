apiVersion: apps/v1
kind: Deployment
metadata:
  name: fitverse-frontend
  labels:
    app: fitverse-frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: fitverse-frontend
  template:
    metadata:
      labels:
        app: fitverse-frontend
    spec:
      containers:
      - name: frontend
        image: xjohnfit/fitverse-frontend:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "100m"
        livenessProbe:
          httpGet:
            path: /
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
