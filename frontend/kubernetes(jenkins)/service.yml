apiVersion: v1
kind: Service
metadata:
  name: fit-verse-frontend
  labels:
    app: fit-verse-frontend
spec:
  ports:
  - port: 5005
    targetPort: 5005
    nodePort: 30005
  selector:
    app: fit-verse-frontend

  type: NodePort