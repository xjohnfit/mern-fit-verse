apiVersion: apps/v1
kind: Deployment
metadata:
    name: fit-verse-api
spec:
    replicas: 1
    selector:
        matchLabels:
            app: fit-verse-api
    template:
        metadata:
            labels:
                app: fit-verse-api
        spec:
            containers:
                - name: fit-verse-api
                  image: xjohnfit/fitverse-api:1.0.0-14
                  resources:
                      limits:
                          cpu: '1'
                          memory: '512Mi'
                      requests:
                          cpu: '200m'
                          memory: '256Mi'
                  ports:
                      - containerPort: 5004
                  envFrom:
                      - secretRef:
                            name: fit-verse-backend-secrets
                  livenessProbe:
                      httpGet:
                          path: /api/health
                          port: 5004
                      initialDelaySeconds: 30
                      periodSeconds: 30
                      timeoutSeconds: 10
                      failureThreshold: 3
                  readinessProbe:
                      httpGet:
                          path: /api/health
                          port: 5004
                      initialDelaySeconds: 5
                      periodSeconds: 10
                      timeoutSeconds: 5
                      failureThreshold: 3
            imagePullSecrets:
                - name: regcred
